import 'package:flutter/material.dart';

class RateProductsScreen extends StatelessWidget {
  const RateProductsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
